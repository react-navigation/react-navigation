appId: ${APP_ID}
---
- stopApp
- runFlow:
    when:
      visible: ".*not responding.*"
    commands:
      - tapOn: "Wait"
- retry:
    maxRetries: 3
    commands:
      - openLink:
          link: ${APP_SCHEME}${LINK}
      - extendedWaitUntil:
          visible: ${TEXT}
          timeout: 60000
